# Contributing

Create a focused branch, add tests, use the installed pre-commit hook (which runs `npm run validate:commit`), and use Conventional Commits. Never commit secrets. Database changes require reviewed migrations.
